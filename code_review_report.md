# รายงานการตรวจสอบ Code (Code Review Report)

จากการตรวจสอบโครงสร้างและโค้ดของโปรเจกต์ `i-love-my-job-main` พบประเด็นสำคัญที่ควรแก้ไขดังนี้ครับ:

## 🚨 1. ปัญหาความปลอดภัย (Security Vulnerabilities) - **สำคัญมาก**

### 1.1 Backend ไม่มีการตรวจสอบ Authentication (Broken Authentication)
- **ปัญหา:** แม้ว่าหน้าบ้าน (Frontend) จะมีการส่ง Token ไปกับ Header (`Authorization: Bearer ...`) แต่ที่หลังบ้าน (Backend) ในไฟล์ API ต่างๆ (เช่น `server/api/GET/Courses.php`, `server/api/GET/get_rooms.php`) **ไม่มีการตรวจสอบ Token นี้เลย**
- **ความเสี่ยง:** ใครก็ตามที่รู้ URL ของ API สามารถดึงข้อมูลหรือแก้ไขข้อมูลได้โดยไม่ต้องล็อกอิน
- **คำแนะนำ:** ต้องสร้างฟังก์ชัน Middleware ใน PHP เพื่อตรวจสอบ JWT Token หรือ Session ก่อนอนุญาตให้เข้าถึง API

### 1.2 Token ที่สร้างขึ้นไม่มีความปลอดภัย
- **ปัญหา:** ในไฟล์ `server/api/POST/login.php` มีการสร้าง Token ด้วย `bin2hex(random_bytes(32))` ซึ่งเป็นแค่สตริงสุ่มธรรมดา ไม่ใช่ JWT และไม่ได้ถูกเก็บลงฐานข้อมูลเพื่อตรวจสอบภายหลัง
- **ความเสี่ยง:** Token นี้ไม่สามารถใช้ยืนยันตัวตนได้จริงใน Request ถัดไป
- **คำแนะนำ:** เปลี่ยนไปใช้ **JWT (JSON Web Token)** เพื่อให้สามารถตรวจสอบข้อมูลผู้ใช้และวันหมดอายุของ Token ได้อย่างปลอดภัย

### 1.3 ข้อมูลการเชื่อมต่อฐานข้อมูล (Database Credentials)
- **ปัญหา:** มีการ Hardcode ชื่อผู้ใช้และรหัสผ่าน (`root`, `''`) ไว้ในไฟล์หลายที่ (`server/config.php` และ `server/api/conn.php`)
- **ความเสี่ยง:** หากไฟล์หลุดออกไป หรือลืมแก้ตอนขึ้น Production จะทำให้ฐานข้อมูลไม่ปลอดภัย
- **คำแนะนำ:** ควรใช้ `.env` file หรือรวมการตั้งค่าไว้ที่เดียวและดึงค่าจาก Environment Variable

## 🛠 2. โครงสร้างและคุณภาพโค้ด (Code Quality & Architecture)

### 2.1 โค้ดซ้ำซ้อน (Redundancy)
- **ปัญหา:** ไฟล์ `server/api/conn.php` และ `server/config.php` มีการประกาศตัวแปรเชื่อมต่อฐานข้อมูลซ้ำกัน
- **คำแนะนำ:** ให้ `conn.php` เรียกใช้ค่าจาก `config.php` เพียงที่เดียว เพื่อให้แก้ไขง่าย

### 2.2 การจัดการ CORS ซ้ำซ้อน
- **ปัญหา:** มีการตั้งค่า CORS ทั้งใน `server/cors.php` และ `server/api/conn.php` (แม้จะ comment ไว้บ้าง)
- **คำแนะนำ:** รวม logic การทำ CORS ไว้ที่ไฟล์เดียว (เช่น `cors.php`) และ include เข้าไปในทุก API ที่ต้องใช้

### 2.3 การตั้งชื่อไฟล์ไม่สม่ำเสมอ (Inconsistent Naming)
- **ปัญหา:** ในโฟลเดอร์ `server/api/GET/` มีทั้ง `Courses.php` (PascalCase), `get_rooms.php` (snake_case), `Getstudyplan.php` (Mixed)
- **คำแนะนำ:** ควรตกลงมาตรฐานการตั้งชื่อให้เหมือนกันทั้งโปรเจกต์ (แนะนำ `snake_case` สำหรับ PHP หรือ `kebab-case` สำหรับ URL)

### 2.4 Frontend Hardcoded Values
- **ปัญหา:** ใน `react/src/Page/Courseinfo.jsx` มีการ Hardcode ชื่อหมวดวิชา เช่น "1.หมวดวิชาสมรรถนะแกนกลาง"
- **ความเสี่ยง:** หากชื่อในฐานข้อมูลเปลี่ยน หน้าเว็บจะแสดงผลผิดพลาดทันที
- **คำแนะนำ:** ควรดึงข้อมูลหมวดวิชาจาก API หรือเก็บเป็น Constants ที่แก้ไขได้ง่าย

## ✅ สรุปและสิ่งที่ควรทำทันที

1.  **แก้ระบบ Login ด่วน:** เปลี่ยนไปใช้ JWT และเขียนฟังก์ชันตรวจสอบ Token ในทุกไฟล์ API
2.  **รวม Config:** ย้าย Database config ไปไว้ที่เดียว
3.  **Clean Code:** ลบโค้ดที่ไม่ได้ใช้และจัดระเบียบชื่อไฟล์

หากต้องการให้ผมช่วยแก้จุดไหนเป็นพิเศษ (เช่น การทำ JWT Login หรือการจัดระเบียบไฟล์) บอกได้เลยครับ!
